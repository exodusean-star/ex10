server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /company/ {
        root /usr/share/nginx/html;
        index index.html;
    }
}
